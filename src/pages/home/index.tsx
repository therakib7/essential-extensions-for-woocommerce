/**
 * External dependencies
 */
/**
 * Internal dependencies
 */
import Dashboard from '@/components/dashboard';
import Layout from '@/components/layout/Layout';

const Home = () => {
    return (
        <Layout title={<></>} slug="home">
            <Dashboard />
        </Layout>
    );
};

export default Home;
